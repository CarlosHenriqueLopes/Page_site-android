@charset "UTF-8";

@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap');

/*
tipos de format{}
- opentype (otf)
- truetype (ttf)
- enbedded-opentype
- truetype-aat ()apple advanced typography)
- svg
*/
@font-face {
    font-family: 'android';
    src: url('../fontes/idroid.otf') format('opentype');
    font-weight: normal;
}

/*cor principal #3DDC84*/
/*fonts - Bebas Neue - android*/
/*OBS: todas as variaveis começar com --*/
:root{
    --cor0: #c5edb6;
    --cor1: #83e1ad;
    --cor2: #3ddc84;
    --cor3: #2fa866;
    --cor4: #1a5c37;
    --cor5: #063d1e;

    --font-padrao: Arial, Verdana, Helvetica, sans-serif;
    /*font-fammily -> bebas*/
    --bebas: 'Bebas Neue', 'cursive';
    --android: 'android', 'cursive';
}

*{
    margin: 0%;
    padding: 0%;
}

body{
    background-color: var(--cor1);
    font-family: var(--font-padrao);
}

p{
    margin: 15px 0px 15px 0px;
    text-align: justify;
    text-indent: 30px;
    line-height: 2em;
    font-size: 1em;
}

header{
    background-image: linear-gradient(to top, var(--cor5), var(--cor3));
    min-height: 160px;
    padding-top: 30px;
}

header > h1{
    color: aliceblue;
    font-family: var(--bebas);
    font-size: 3em;
    text-align: center;
    padding-bottom: 20px;
    text-shadow: 2px 2px 5px black;
}

header > p{
    color: aliceblue;
    text-align: center;
    font-family: var(--font-padrao);
    font-size: 1.2em;
    text-shadow: 2px 2px 5px black;
    margin-bottom: 30px;
}

nav{
    background-color: var(--cor5);
    padding: 10px;
    box-shadow: 0px 2px 2px rgba(0, 0, 0, 0.34);
}

nav > a{
    color: var(--cor0);
    padding: 10px;
    text-decoration: none;
    font-weight: bold;
    border-radius: 5px;
    transition-duration: 0.5s;
}

a.externo::after{
    content: '\00a0\1F517';
}

nav > a:hover{
    background-color: #1a5c37;
    text-shadow: 0px 0px 6px #83e1ad;
}

main h1{
    color: var(--cor5);
    font-family: var(--android);
    font-weight: normal;
    font-size: 1.8em;
}

main h2{
    font-family: var(--android);
    color: var(--cor4);
    font-size: 1.3em;
    font-weight: normal;
    background-image: linear-gradient(to right, var(--cor1), transparent);
    text-indent: 6px;
}

main img.droids{
    width: 100%;
}

main img.irina{
    width: 100%;
}

main img.pequena{
    display: block;
    max-width: 350px;
    margin: auto;
}

div.video{
    background-color: var(--cor5);
    margin: 0px -20px 30px -20px;
    padding: 20px;
    padding-bottom: 59%;

    position: relative;
}

div.video > iframe{
    position: absolute;
    top: 5%;
    left: 5%;
    width: 90%;
    height: 90%;
}

main strong{
    color: var(--cor3);
    font-weight: bold;
    text-shadow: 0px 0px 10px var(--cor0);
}

main a{
    text-decoration: underline;
    color: var(--cor4);
    font-weight: bold;
}

main a:hover{
    text-decoration: none;
    background-color: var(--cor5);
    color: aliceblue;
    text-shadow: 1px 1px 10px var(--cor1);
    border-radius: 5px;
    font-weight: bold;
    padding: 3px;

}

main{
    background-color: aliceblue;
    min-width: 300px;
    max-width: 1000px;
    margin: auto;
    margin-bottom: 30px;
    border-radius: 0px 0px 10px 10px;
    padding: 20px;
    box-shadow: 0px 0px 100px rgba(0, 0, 0, 0.489);
}

footer{
    background-color: var(--cor5);
    padding: 5px;
}

footer > p{
    color: aliceblue;
    text-align: center;
    font-size: 1em;
}

footer > p:hover{
    text-shadow: 0px 0px 10px var(--cor0);
}

aside{
    background-color: var(--cor0);
    padding: 10px;
    border-radius: 10px;
    box-shadow: 0.5px 0.5px 6px rgba(0, 0, 0, 0.438);
}

aside > h3{
    background-color: var(--cor3);
    color: aliceblue;
    font-weight: bold;
    margin: -10px -10px 0 -10px;
    border-radius: 10px 10px 0px 0px;
    text-indent: 3%;
}

aside > ul{
    list-style-position: inside;
    columns: 2;
    list-style-type: '\2705\00a0'; /*um jeito de add emojis (\00a0 espaço)*/
}